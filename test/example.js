let assert = require('assert');
describe('Example', function() {
  describe('#SampleTest()', function() {
    it('This is just a sample test that should  automatically pass', function() {
      assert.equal(1, 1);
    });
  });
});
