var assert = require('assert');
describe('Example', function() {
  describe('#SampleTest()', function() {
    it('This is just a sample test that should be automatically passed', function() {
      assert.equal(1, 1);
    });
  });
});